apiVersion: v1
kind: Service
metadata:
  name: client
  labels:
    app: aegis
    tier: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: client
  type: ClusterIP
