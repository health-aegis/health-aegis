apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: aegis
  labels:
    app: api-gateway
spec:
  selector:
    app: api-gateway
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  type: ClusterIP
