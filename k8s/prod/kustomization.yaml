apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aegis-prod

resources:
  - namespace.yaml
  - ../../base
  - hpa.yaml

patches:
  - path: patches/resources.yaml
    target:
      kind: Deployment
      name: api-gateway
