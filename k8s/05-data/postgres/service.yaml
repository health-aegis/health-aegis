apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: aegis
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None   # Headless for StatefulSet DNS resolution
