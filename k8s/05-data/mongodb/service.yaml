apiVersion: v1
kind: Service
metadata:
  name: mongodb
  labels:
    app: aegis
    tier: database
spec:
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  selector:
    app: mongodb
  type: ClusterIP
