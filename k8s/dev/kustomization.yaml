apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aegis-dev

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: patches/replicas.yaml
    target:
      kind: Deployment
      name: api-gateway
